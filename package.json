{
  "name": "{{ name }}",
  "version": "1.0.0",
  "description": "{{ description }}",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "{{ author }}",
  "license": "ISC",
  "bin": {
    "my-demo": "index.js"
  },
  "dependencies": {
    "commander": "^4.1.1",
    "download-git-repo": "^3.0.2"
  }
}
